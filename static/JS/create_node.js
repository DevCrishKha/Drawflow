/*
    
        USE THIS JS FILE ONLY AFTER SUCCESSFUL BUILDING OF THE PROJECT

Imported inside js files who need the editor object
This js file takes the mouse location when the drop event listener of a div draggable="true" is triggered
    Then a node is created using the editor.addNode() method with its corder at the position of the mouse when the div is dropped. The node is created in the drawflow editor (or the workspace) which is an object inside the DOM.
*/